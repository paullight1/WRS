# Domain

Pure WRS business rules and domain types live here. Domain modules must not import React, browser globals, network clients, database SDKs, or deployment-provider code. New domain code is TypeScript-first, deterministic, unit-tested, linted, formatted, and typechecked.
